
/**
 * Defines a set of required methods
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public interface canCalculate
{
   // a set of abstract method definitions
  public int add (int i, int j);{
  int result = 0;
        for(int k = 1; k<= i; k++){
            result++;
        }
        for(int l = 1; l<= i; l++){
            result++;
        }
        return result;
    
  public double multiply(double x, double y);{
    
    }
}
