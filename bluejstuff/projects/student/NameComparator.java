
/**
 * Write a description of class Comparator here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class NameComparator implements Comparator<FirstNameComparator>

{ 
   
    //what is going on theres 4 compare classess or something
    //Collection.sort(student, new NameComparator());
    for(Student Student:myArr) Object 02){
        print(Student.getAge());
}
    public int compareTo(Student obj1){
        if(age==obj1.getAge()){
            return 0;
        }
        else if (age<obj1.getAge()){
            return -1;
        }
        else{
            return 1;
        }
    }
}
